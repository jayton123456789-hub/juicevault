# ═══════════════════════════════════════════════════
# JuiceVault — Environment Variables
# ═══════════════════════════════════════════════════
# Copy this to .env and fill in your values.
# On Render.com, set these in the Environment tab.

NODE_ENV=production
PORT=10000

# PostgreSQL — Render provides this automatically
DATABASE_URL=postgresql://user:password@host:5432/juicevault

# Redis — OPTIONAL (app works fine without it, used for caching)
REDIS_URL=

# JWT — Use a long random string in production!
JWT_SECRET=CHANGE_ME_to_a_random_64_char_string
JWT_EXPIRES_IN=7d

# Juice WRLD API — Do NOT change this
JUICEWRLD_API_BASE=https://juicewrldapi.com/juicewrld

# Typesense — Not required for basic deployment
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_API_KEY=unused

# Frontend URL (for CORS) — Set to your Render URL
FRONTEND_URL=https://juicevault.onrender.com

# AssemblyAI — For lyrics timing generation (optional)
# Get a free key at https://www.assemblyai.com (185 hours free)
ASSEMBLYAI_API_KEY=
